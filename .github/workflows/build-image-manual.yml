name: Build Image Manual Branch

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to build and release'
        required: true
        default: 'develop'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          ref: ${{ github.event.inputs.branch }}

      - name: Build Frontend Docker Image
        run: cd services/frontend && docker build . --file Dockerfile --tag ghcr.io/justlabv1/justwms:frontend-${{ github.sha }} --tag ghcr.io/justlabv1/justwms:frontend-${{ github.ref_name }}

      - name: Build Backend Docker Image
        run: cd services/backend && docker build . --file Dockerfile --tag ghcr.io/justlabv1/justwms:backend-${{ github.sha }} --tag ghcr.io/justlabv1/justwms:backend-${{ github.ref_name }}

      - name: Build JustWMS Docker Image
        run: docker build . --file Dockerfile --tag ghcr.io/justlabv1/justwms:${{ github.sha }} --tag ghcr.io/justlabv1/justwms:${{ github.ref_name }}

      - name: 'Login to GitHub Container Registry'
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}

      - name: Push Docker Images
        run: |
          docker push ghcr.io/justlabv1/justwms:frontend-${{ github.sha }}
          docker push ghcr.io/justlabv1/justwms:frontend-${{ github.ref_name }}
          docker push ghcr.io/justlabv1/justwms:backend-${{ github.sha }}
          docker push ghcr.io/justlabv1/justwms:backend-${{ github.ref_name }}
          docker push ghcr.io/justlabv1/justwms:${{ github.sha }}
          docker push ghcr.io/justlabv1/justwms:${{ github.ref_name }}
