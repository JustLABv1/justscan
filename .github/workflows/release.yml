name: Build and Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build Frontend Docker Image
        run: cd services/frontend && docker build . --file Dockerfile --tag github.com/justnz/justwms:frontend-latest --tag github.com/justnz/justwms:frontend-${{ github.sha }} --tag github.com/justnz/justwms:frontend-${{ github.ref_name }}

      - name: Build Backend Docker Image
        run: cd services/backend && docker build . --file Dockerfile --tag github.com/justnz/justwms:backend-latest --tag github.com/justnz/justwms:backend-${{ github.sha }} --tag github.com/justnz/justwms:backend-${{ github.ref_name }}

      - name: Build JustNZ Docker Image
        run: docker build . --file Dockerfile --tag github.com/justnz/justwms:latest --tag github.com/justnz/justwms:${{ github.sha }} --tag github.com/justnz/justwms:${{ github.ref_name }}

      - name: 'Login to GitHub Container Registry'
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}

      - name: Push Docker Images
        run: |
          docker push github.com/justnz/justwms:frontend-latest
          docker push github.com/justnz/justwms:frontend-${{ github.sha }}
          docker push github.com/justnz/justwms:frontend-${{ github.ref_name }}
          docker push github.com/justnz/justwms:backend-latest
          docker push github.com/justnz/justwms:backend-${{ github.sha }}
          docker push github.com/justnz/justwms:backend-${{ github.ref_name }}
          docker push github.com/justnz/justwms:latest
          docker push github.com/justnz/justwms:${{ github.sha }}
          docker push github.com/justnz/justwms:${{ github.ref_name }}

  release:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: Create Release
        id: create_release
        uses: ncipollo/release-action@v1
        with:
          name: Release ${{ github.ref_name }}
          tag: ${{ github.ref_name }}
          skipIfReleaseExists: true
          generateReleaseNotes: true
          token: ${{ secrets.ACCESS_TOKEN }}
